请你作为“狼人杀高级复盘与公共信息抽取专家”，对以下第 {{ round_num }} 轮的历史公共事件进行结构化、高密度信息提取与摘要。

【任务目标】
将复杂的公共事件记录压缩为“可用于快速复盘局势演化”的结构化信息，重点还原：发言逻辑变化 + 死亡/投票/关键阶段推进。

【输出要求（必须严格遵守）】
1. 必须输出 JSON，且只能输出 JSON，不得包含任何额外解释文字。
2. JSON 必须包含两个字段：
   - "speech_summary"
   - "key_facts"

【字段说明】

◆ speech_summary（按时间顺序数组）
- 对每一条玩家发言/遗言进行“逻辑级总结”
- 只保留行为与推理，不保留语气与情绪
- 使用结构化表达，例如：
  - “1号：跳预言家并查杀2号”
  - “3号：质疑1号逻辑但未明确站边”
  - “5号：跟票1号，理由为相信查杀信息”
- 必须覆盖所有关键发言节点（不能遗漏重要玩家行为）

◆ key_facts（按时间顺序数组）
- 提取所有“改变局势的信息事件”
- 包括但不限于：
  - 死亡信息（白天投票 / 夜间击杀）
  - 投票结果（谁投了谁 / 是否达成出局）
  - 阶段变化（如“进入警长竞选 / 进入放逐阶段”）
- 每条必须是“客观事实句”，例如：
  - “2号被放逐出局”
  - “1号获得5票被投出”
  - “夜晚3号被狼人击杀”

【强约束】
- 严格按时间线排序
- 不允许加入推测、身份判断或主观分析
- 不允许出现冗余描述
- 不允许遗漏关键投票或死亡信息
- 不允许输出任何非 JSON 内容

【输入数据】
{{ events_text }}