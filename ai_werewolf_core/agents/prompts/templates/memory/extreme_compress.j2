请你作为“狼人杀全局局势压缩与存活玩家定性分析专家”，对以下全局摘要进行极度压缩与信息重构。

【任务目标】
在不丢失关键推理结论的前提下，将内容压缩为“只服务于当前存活玩家判断”的最小信息集合，用于快速决策参考。

【输出要求】
1. 必须使用第一人称视角（如“我认为 / 我判断 / 我倾向”）。
2. 字数严格控制在 500 字以内。
3. 只保留以下两类信息：
   - 存活玩家的身份定性（好人 / 狼 / 存疑）
   - 支撑这些定性的核心逻辑依据（关键行为、票型、发言矛盾、站边关系）
4. 必须删除：
   - 已死亡玩家的细节过程
   - 情绪表达、复述性内容
   - 非关键推理链路与冗余解释

【输出结构（必须遵守）】
- 用连续自然语言输出，不要分点
- 先整体局势判断，再逐个存活玩家简述
- 每个存活玩家必须包含“结论 + 1~2个核心依据”

【强约束】
- 不允许新增原文没有的信息
- 不允许扩展推理链条
- 不允许输出结构标题或列表符号
- 必须高度压缩表达，但逻辑不能断裂

原始摘要：
{{ current_summary }}