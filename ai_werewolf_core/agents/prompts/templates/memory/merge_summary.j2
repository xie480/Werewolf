```text id="g7m1xq"
请你作为“狼人杀全局信息融合与动态复盘专家”，将新增信息无损融入已有全局摘要，并生成一份更新后的全局局势总结。

【输入】
旧全局摘要：
{{ current_summary }}

第 {{ round_num }} 轮新增事件与推理：
{{ new_info }}

【任务目标】
在保持推理连续性的前提下，更新对全局局势与存活玩家身份判断，使其始终反映“最新可信信息状态”。

【输出要求】
1. 必须输出更新后的“全局摘要”，不得分段输出多个版本。
2. 必须保持第一人称视角（如“我认为 / 我判断 / 我倾向”）。
3. 核心重点始终聚焦：
   - 当前存活玩家的身份判断（好人 / 狼 / 存疑）
   - 支撑这些判断的关键逻辑线索（票型、发言矛盾、行为链路、站边关系）
4. 对已死亡玩家的信息处理方式：
   - 可保留“关键遗留信息”
   - 删除重复、背景性、过程性细节
   - 只保留对当前局势仍有影响的线索

【压缩与更新规则】
- 新信息优先级高于旧信息冲突部分
- 若新旧信息冲突，必须体现“我更新判断为……原因是……”
- 不允许简单拼接旧摘要与新信息
- 必须进行结构重写与信息融合，而非追加

【强约束】
- 不得新增原文未提供的事实
- 不得输出分析过程
- 不得使用列表或标题
- 控制整体表达在“紧凑但完整”的摘要长度（通常 300~600 字）

【输出】
仅输出更新后的全局摘要正文：
```
