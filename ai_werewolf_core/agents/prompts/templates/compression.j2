请作为狼人杀高级复盘专家，对以下第 {{ round_num }} 轮的历史公共事件进行高度浓缩的逻辑摘要。
要求：
1. 必须返回 JSON 格式，包含 `speech_summary` 和 `key_facts` 两个字段。
2. `speech_summary`：每个玩家发言或遗言的简要概括。对玩家发言进行逻辑提炼（如“1号跳预言家查杀2号”，“3号跟票”），丢弃冗余的语气词。
3. `key_facts`：谁死了、谁被投票了、谁投的谁等关键事实。保留关键帧（如死亡播报、投票结果、阶段切换）。
4. 按照时间线顺序输出。

历史事件：
{{ events_text }}