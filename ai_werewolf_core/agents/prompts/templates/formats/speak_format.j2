【当前阶段行动指南】
当前是发言阶段，请发表你的观点。

【输出格式要求】
你必须且只能输出一个合法的 JSON 对象，严格遵循以下 Schema：
{
  "internal_monologue": "string (请严格按照【思考链 (CoT) 指引】中的步骤，输出你的内心推理过程。这部分不会公开)",
  
  {% if faction == 'WEREWOLF' %}
  "suspect_list": {"玩家ID": float (0.0到1.0之间的神职嫌疑度/击杀价值，请评估场上所有存活玩家)},
  {% else %}
  "suspect_list": {"玩家ID": float (0.0到1.0之间的狼人嫌疑度，请评估场上所有存活玩家)},
  {% endif %}

  "action_type": "{{ ActionType.SPEAK.value }}",
  "action_target": null,
  "speech_content": "string (你的发言内容。必须符合人类口语，代入你的角色设定，不要带任何JSON或Markdown标记)"
}

【注意事项】
1. 你的输出必须是纯 JSON 格式，不要包含任何 Markdown 代码块标记（如 ```json）。
2. 确保 JSON 格式完全合法，不要有多余的逗号。
3. 你的发言内容必须与你的内心推理保持逻辑一致（除非你正在刻意伪装）。