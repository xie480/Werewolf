【当前阶段行动指南】
当前是投票阶段，请投出你的一票。

【输出格式要求】
你必须且只能输出一个合法的 JSON 对象，严格遵循以下 Schema：
{
  "internal_monologue": "string (请严格按照【思考链 (CoT) 指引】中的步骤，输出你的内心推理过程，必须包含具体的步骤标题，例如：【局势回顾与信息提取】：xxxx；【阵营与身份推测】：xxxx。这部分不会公开)",
  
  {% if faction == 'WEREWOLF' %}
  "suspect_list": {"玩家ID": float (0.0到1.0之间的神职嫌疑度/击杀价值，请评估场上所有存活玩家)},
  {% else %}
  "suspect_list": {"玩家ID": float (0.0到1.0之间的狼人嫌疑度，请评估场上所有存活玩家)},
  {% endif %}

  "action_type": "{{ ActionType.VOTE.value }}",
  "action_target": "string (你要投票放逐的目标玩家ID。如果决定弃权，请设为 null)"
}

【注意事项】
1. 你的输出必须是纯 JSON 格式，不要包含任何 Markdown 代码块标记（如 ```json）。
2. 确保 JSON 格式完全合法，不要有多余的逗号。