# 白天行动结算与投票管理器设计

## 1. 架构定位与设计目标

在狼人杀对局中，白天的行动与夜晚有着本质的区别：
- **夜晚行动**（狼刀、女巫用药、预言家验人）具有严格的因果链，需要**串行提交、延迟统一结算**。
- **白天行动**（放逐投票、猎人开枪、白痴翻牌等）通常是**即时结算**或**条件触发**的，不需要复杂的草稿暂存机制。

为了遵循单一职责原则 (SRP) 并保持 `GameEngine` 主流程的清晰，我们将白天的核心逻辑拆分为两个独立的管理器：
1. **投票管理器 (`VoteManager`)**：专职处理群体性的投票行为（放逐投票、PK 投票、警长竞选投票）。
2. **特殊行动结算器 (`SpecialActionResolver`)**：专职处理白天特殊角色的即时技能插队（猎人开枪、白痴翻牌等）。

## 2. 核心组件设计

### 2.1 投票管理器 (VoteManager)

**文件路径**: `ai_werewolf_core/core/engine/vote_manager.py`

**核心职责**:
- **选票收集与校验**：记录投票意图，校验投票人是否存活、是否在合法的投票阶段、目标是否合法（如 PK 阶段只能投 PK 名单上的人）。
- **计票与平票处理**：统计最高票。如果出现平票，返回平票名单供引擎触发 PK 流程。
- **即时结算**：如果产生唯一最高票，直接调用目标的 `die()` 方法，并发布死亡事件。

**核心接口设计**:
```python
from typing import Dict, List, Optional
import structlog
from ai_werewolf_core.core.event.bus import EventBus
from ai_werewolf_core.schemas.enums import GamePhase, ActionType, EventType, Visibility
from ai_werewolf_core.schemas.models import AgentAction, Event
from ai_werewolf_core.core.engine.roles.base import BaseRole
from ai_werewolf_core.core.engine.exceptions import ActionValidationError

logger = structlog.get_logger(__name__)

class VoteResolveResult:
    """投票结算结果"""
    def __init__(self, is_tie: bool, highest_voted: List[str], vote_details: Dict[str, str]):
        self.is_tie = is_tie
        self.highest_voted = highest_voted
        self.vote_details = vote_details

class VoteManager:
    def __init__(self, game_id: str, event_bus: EventBus):
        self.game_id = game_id
        self.event_bus = event_bus
        self.current_votes: Dict[str, str] = {}  # voter_id -> target_id
        self.pk_candidates: Optional[List[str]] = None # 当前 PK 名单
        self._logger = logger.bind(game_id=game_id, module="VoteManager")

    def begin_vote(self, pk_candidates: Optional[List[str]] = None) -> None:
        """开启新一轮投票，清空历史选票。"""
        self.current_votes.clear()
        self.pk_candidates = pk_candidates
        self._logger.info("vote_begin", pk_candidates=pk_candidates)

    def submit_vote(self, action: AgentAction, roles: Dict[str, BaseRole], current_phase: GamePhase) -> bool:
        """
        提交选票。
        
        **Why**: 校验投票合法性（如 PK 阶段只能投 PK 候选人），并记录选票。
        """
        if action.action_type != ActionType.VOTE:
            raise ActionValidationError(action, "非投票动作")
            
        voter = roles.get(action.actor_id)
        if not voter or not voter.is_alive:
            raise ActionValidationError(action, "投票人不存在或已死亡")
            
        if self.pk_candidates and action.target_id not in self.pk_candidates:
            raise ActionValidationError(action, f"只能投给 PK 候选人: {self.pk_candidates}")
            
        self.current_votes[action.actor_id] = action.target_id
        self._logger.info("vote_submitted", voter_id=action.actor_id, target_id=action.target_id)
        return True

    async def resolve_vote(self, roles: Dict[str, BaseRole]) -> VoteResolveResult:
        """
        结算投票。
        
        **Why**: 统计票数，处理平票逻辑。如果有唯一最高票，直接执行死亡逻辑并发布事件。
        """
        # 统计逻辑...
        pass
```

### 2.2 特殊行动结算器 (SpecialActionResolver)

**文件路径**: `ai_werewolf_core/core/engine/special_action_resolver.py`

**核心职责**:
- **条件校验**：判断当前是否处于允许特殊行动的阶段（如 `HUNTER_SHOOT`），以及角色是否满足发动条件。
- **即时结算**：接收到动作后，立刻执行状态变更（如目标死亡）。
- **中断恢复**：特殊行动结束后，返回结果供引擎决定下一个阶段。

**核心接口设计**:
```python
class SpecialActionResolver:
    def __init__(self, game_id: str, event_bus: EventBus):
        self.game_id = game_id
        self.event_bus = event_bus
        self._logger = logger.bind(game_id=game_id, module="SpecialActionResolver")

    async def handle_action(self, action: AgentAction, roles: Dict[str, BaseRole], current_phase: GamePhase) -> bool:
        """
        处理特殊行动（如猎人开枪）。
        
        **Why**: 特殊行动是即时生效的，不需要等待阶段结束。校验通过后立即执行副作用（如杀人）并发布事件。
        """
        if current_phase == GamePhase.HUNTER_SHOOT and action.action_type == ActionType.HUNTER_SHOOT:
            return await self._handle_hunter_shoot(action, roles)
        return False
        
    async def _handle_hunter_shoot(self, action: AgentAction, roles: Dict[str, BaseRole]) -> bool:
        # 校验猎人状态、目标状态
        # 执行死亡
        # 发布事件
        pass
```

## 3. GameEngine 调度逻辑重构

在 `GameEngine` 中，根据当前阶段将动作路由到不同的管理器：

```python
# ai_werewolf_core/core/engine/main.py (伪代码)

async def submit_action(self, action: AgentAction):
    phase = self.state_machine.current_phase
    
    # 1. 夜晚行动 -> 交给 NightResolver (延迟结算)
    if phase in (GamePhase.NIGHT_WOLF_ACT, GamePhase.NIGHT_WITCH_ACT, GamePhase.NIGHT_SEER_ACT):
        self.night_resolver.submit_action(action, self.roles, phase)
        
    # 2. 投票行动 -> 交给 VoteManager (收集选票)
    elif phase in (GamePhase.DAY_VOTE, GamePhase.DAY_PK_VOTE):
        self.vote_manager.submit_vote(action, self.roles, phase)
        
    # 3. 特殊行动 -> 交给 SpecialActionResolver (即时结算)
    elif phase in (GamePhase.HUNTER_SHOOT,):
        await self.special_action_resolver.handle_action(action, self.roles, phase)
        # 引擎根据结果推进状态机
        await self.lifecycle.advance_phase(GamePhase.LAST_WORDS)
        
    # 4. 其他常规行动 (如发言)
    elif phase in (GamePhase.DAY_DISCUSSION, GamePhase.LAST_WORDS):
        self.handle_speak(action)
```

## 4. 遵循的架构规范 (agent.md)

1. **规则硬编码**: 投票平票逻辑、猎人开枪条件等全部在 Python 代码中硬编码，不依赖 LLM 判定。
2. **无魔法字符串**: 所有阶段 (`GamePhase`)、动作类型 (`ActionType`) 均使用 `schemas/enums.py` 中的枚举。
3. **强类型声明**: 接口参数和返回值严格使用 Type Hinting 和 Pydantic/DataClass 模型（如 `VoteResolveResult`）。
4. **异步优先**: 结算过程中涉及事件发布 (`event_bus.publish`)，全部使用 `async/await`。
5. **结构化日志**: 使用 `structlog` 记录投票明细、平票情况、特殊行动触发等关键节点，并注入 `game_id` 等上下文。
6. **注释规范**: 核心方法（如 `resolve_vote`）必须包含 `**Why**` 注释，解释为什么采用即时结算或平票处理的业务逻辑。
